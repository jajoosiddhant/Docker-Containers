# Base image/version variable is the Image to pull. See Dockerfile
BASE_IMAGE_NAME=ubuntu
BASE_IMAGE_VERSION=latest
# Container name/version variable is the name/version of the container to use as an argument to docker stop/run. See build-docker.sh/run-docker.sh
CONTAINER_NAME=ubuntu
CONTAINER_VERSION=20.04
CONTAINER=${CONTAINER_NAME}:${CONTAINER_VERSION}